1.4.6

[New Functions & Features]
+ Added configureClientSelection() interactive Step 1 prompt for Normal Roblox (single instance full window), JEP Clones, or Other Clones
+ Added checkForAccountLockOrChallenge() proactive scanner for Arkose/FunCaptcha, 2-Step Verification & account security holds
+ Added handleAccountLockAbort() immediately halting clone launches and isolating locked accounts from recovery cycles
+ Added drawAccountLockAlertCard() centered terminal alert box detailing account holds and providing actionable recovery steps
+ Added quarantineClone() thread-safe clone isolation with purgeFromRecoveryQueue() preventing infinite auto-rejoin loops
+ Added in-game HTTP /report and logcat real-time account security hold detection
+ Added dedicated rich Discord alert embeds with step-by-step instructions for clearing verification on physical devices
+ Added detectVirtualDevice() & applyHardwareSpoofing() Anti-VM and Samsung Galaxy S23 hardware camouflage
+ Added installTermuxBootHook() & saveSessionConfig() Termux:Boot auto-start upon phone restart
+ Added pickPopularGame() pre-configured game preset selector with 12 popular Roblox experiences
+ Added configureSystemOptimizer() interactive wizard step [1-3] for Deep Clean, Light Clean, or Skip
+ Added optimizeSystemAndCleanApps() 3-stage memory reclamation, cache flush & background app killer
+ Added isProtectedPackage() safe whitelist guard protecting Termux, root managers, System UI & IMEs
+ Added configureTargetExperience() with per-clone Custom Game & VIP link configuration (Mixed Mode)
+ Added getInstalledDeltaVersion() & getDisplayDeltaVersion() live APK version detection
+ Added extractVersionFromAPK() fallback zip reader parsing AndroidManifest.xml
+ Added isDeltaOutdated() semantic version comparison against official Delta API
+ Added handleDeltaUpgradeAbort() proactive guard stopping clone launching when upgrade dialog appears
+ Added unified SYSTEM VERIFICATION card combining Script Integrity & Delta diagnostics
+ Added automatic terminal resets (clearTerminal) before alert and abort states
+ Fixed custom game selection prompt per clone in multi-instance configuration

[Removed Functions & Deprecated Features]
- Removed hardcoded CurrentDeltaVersion constant
- Removed raw safeLog console spam causing mid-word line-wrapping on narrow mobile screens
- Removed redundant [STOPPED] terminal exit messages
- Removed multi-box startup stacking
